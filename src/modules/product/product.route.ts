import { Router } from 'express';

const router = Router();

// TODO: definir rutas de product
// router.get('/', getAll);
// router.get('/:id', getById);
// router.post('/', auth, create);
// router.put('/:id', auth, update);
// router.delete('/:id', auth, remove);

export default router;
