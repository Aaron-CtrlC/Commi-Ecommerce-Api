import { Router } from 'express';

const router = Router();

// TODO: definir rutas de user
// router.post('/register', register);
// router.post('/login', login);
// router.get('/profile', auth, getProfile);

export default router;
